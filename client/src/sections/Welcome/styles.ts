import { createUseStyles } from 'react-jss';

export const welcomeStyles = createUseStyles({
  container: {
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
